# Gamma

Nested layout. The container checks out `feature/search` so the project
picker Git preview differs from alpha/beta/notes (`main`).

- [src/app/init.lua](src/app/init.lua)
- [src/app/config.lua](src/app/config.lua)
- [tests/spec.lua](tests/spec.lua)

Use this tree to confirm nested files restore into the same splits.
