# Loose

This directory is not a Git repository. Discovery still lists it because
`/workspace/*` is a configured root. Git preview in the picker should be
empty.

- [scratch.txt](scratch.txt)

`:SuperProjectOpen` can also register directories that are not in the
glob, using the directory browser.
